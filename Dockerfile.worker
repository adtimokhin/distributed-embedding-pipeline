# TODO (Stage 3): Complete this Dockerfile.
#
# The worker image must contain TWO things:
#   1. The compiled Go worker binary
#   2. The Python embedder and its dependencies (sentence-transformers)
#
# Use a multi-stage build:
#   Stage 1 ("builder"): golang:1.24-alpine — compile the Go worker binary.
#   Stage 2 (runtime):   python:3.11-slim   — install Python deps, copy binary.
#
